window.INSTORE_CONFIG = {
  // This config will change the max length accepted on the Gift Card modal, can change it if the origin of the gift card is not the VTEX provider.
  customGiftCardLength: 30,
  // This config disable the mmask on the Gift Card modal (SSSS-SSSS-SSSS-SSSS), it's essential if you use a different origin that doesn't allow for the same format.
  disableGiftCardMask: true,
}
